# Open Review Findings

A00 owns this current queue. Closed findings are removed after the fix is verified and any
lasting rule/test/documentation has been placed in its durable home.

Severity: **P0** blocks all affected integration; **P1** blocks the branch; **P2** must be
scheduled; **P3** is improvement.

| ID | Severity | Affected agent(s) | Finding and evidence | Required exit condition | Status |
|---|---|---|---|---|---|
| RF-001 | P1 | A00–A09, A09 integration | Branch identity is inconsistent inside the active docs. `docs/ecom-os/parallel-build/BRANCH-AND-INTEGRATION.md`, goals, handoffs, and builder living docs name `coordination/program` and `agent/A01-platform-foundation` through `agent/A09-production-integration`. The current launch instruction pins A00 to `agent/a00-orchestrator`, and observed branches are `agent/a01-foundation`, `agent/a02-trace-ledger`, `agent/a03-hermes-runtime`, `agent/a04-cs`, `agent/a05-finance`, `agent/a06-ui-system`, `agent/a07-chat`, `agent/a08-ops-briefs`, and `agent/a09-integration`; current local heads now differ by agent, while `origin` still exposes only A00 programme refs. `origin/main:docs/ecom-os/parallel-build/WORKTREE-SETUP-REPORT.md` and `AGENT-LAUNCH-CARDS.md` confirm these observed branches were intentionally launched, but those files are not present in this branch and the normative branch protocol remains stale. | Either bring the launch-card branch map into the active A00 programme docs and update builder living docs to actual branches, or create/standardize the canonical branch names from the parallel-build docs. A09 ready checks must use one authoritative map. | Open |
| RF-002 | P1 | A02, A04, A05 | Baseline CS external writes bypass the v2 action/attempt/trace contract. `backend/app/services/flow_engine.py:431` calls `self.inbox.send_message(...)` directly and commits only a `TicketMessage`; `backend/app/services/flow_engine.py:367` calls `self.shopify.create_discount(...)` directly; `backend/app/services/connectors/composio_inbox.py:154` executes the Composio send tool directly. There is no durable `actions` row, action digest, connector attempt, provider outcome evidence, or `outcome_unknown` path in this baseline code. | A02 exposes durable action/attempt/trace ports; A04 connector writes require exact connection and attempt recording; A05 reply/discount/refund flows call those ports. Duplicate/race/timeout-after-success/reconciliation tests must prove one supported side effect and visible action state. | Open |
| RF-003 | P1 | A02, A04, A05, A09 | Baseline realtime email webhook bypasses durable event acceptance. `backend/app/api/ecom_webhooks.py:50` accepts `/ecom/webhooks/email`, checks a query/header token at lines 57-60, then starts `_run_loop` as a background task at line 62. It does not verify a provider signature against raw body, persist an inbound event before work, preserve provider event identity, or enqueue Postgres-backed processing before agent work. A02 now has a local unpublished draft that persists the realtime email trigger into `DurableInboxEvent`/`DurableJob` before scheduling the legacy loop, with duplicate/retry tests passing, but raw-body provider signature verification and broader provider routes are still not accepted/published. | Inbound provider routes verify raw-body signatures, insert a durable inbox event with source/account/provider ID before processing, acknowledge only after durable acceptance, and process through leased jobs with duplicate tests. | Open |
| RF-004 | P1 | A03, A05 | Baseline Hermes integration is the superseded runtime model, not the v2 HermesBridge contract. `backend/app/services/agent_runtime/base.py:1` defines `AgentRuntime`; `backend/app/services/agent_runtime/hermes.py:43` subclasses `LLMCSRuntime`; it posts to `/delegate` at line 83, falls back to direct Anthropic at lines 60-62, and relies on a scoped `cs` profile/no-refund tool assumption at lines 74-80. A03 now has local unpublished `HermesBridge`, native blocked stub, OpenClaw compat/dev transport, catalog MCP server, conformance gate runner, and 84 passing targeted tests, but no real pinned Hermes endpoint/probe and no published branch evidence. The gate is intentionally RED until a real Hermes endpoint is supplied. | A03 publishes and tests `HermesBridge` with canonical session create/resume/history, background run recovery, capability probe, adapter/MCP catalog, and honest trace coverage. Legacy `AgentRuntime`/`/delegate` paths are fenced as compatibility input or removed from supported Ecom-OS workflows. Real Hermes capability evidence is required before any feature is marked ready. | Open |
| RF-005 | P1 | A01, A04, A05 | Baseline connector/account selection is not exact enough for supported writes. `backend/app/services/cs_loop.py:67-80` loads stores and uses `stores[0]`, then discovers an active mail account and constructs a Composio inbox connection from the returned ID. `backend/app/services/tickets.py:209-215` repeats first-active mail account discovery. This violates the v2 ban on default/latest/most-recent account selection for supported Ecom-OS writes. | Store, inbox, channel, and connector operations are bound to explicit `brand_id`, `store_id`, `connection_id`, actor, and target before execution. Wrong-account and ambiguous-account fixtures reject in every autonomy mode. | Open |
| RF-006 | P1 | A09, any owner considering legacy feature branches | Legacy feature branches are not safe integration candidates. Relative to baseline `3909904`, `feat/ecom-os-slice-1-design-system` changes 316 files and deletes active v2 agent prompts, programme living docs, builder living docs, design docs, migrations, tests, and many backend modules. `feat/ecom-os-slice-2-connections` changes 286 files and also deletes active v2 prompts/programme/living/design docs plus migrations/tests. | Do not integrate either branch as-is. Any useful code must be manually salvaged by the owning agent into its active branch with current living docs, accepted interfaces, migrations, and tests. | Open |
| RF-007 | P2 | A01, A02, A03, A04, A05, A06, A07, A08, A09 | Builder progress remains local/unpublished. `origin` currently exposes only A00 programme refs. A01 has local commit `6658b82`; A03 has local commit `689cf10`; A04 has local commit `46ef05d`; A09 has local commit `6d8b1e3`; A02/A05/A06/A07/A08 have uncommitted worktree diffs. These are useful local evidence but cannot be consumed through `git show origin/<agent-branch>:...` and are not ready-for-integration evidence. | Builders push coherent checkpoints on their assigned branches, including current living docs and verification evidence, or explicitly discard/supersede local drafts. A00 then reconciles programme status against pushed branch evidence. | Open |
| RF-008 | P1 | A06, A08, A00 | Builder worktrees contain edits to A00-owned programme files. A06 and A08 both show modified `docs/ecom-os/living/00-program/INTERFACE-REQUESTS.md`. Parallel-build protocol says A00 may edit only `docs/ecom-os/living/00-program/**` and builders keep their own living docs current. | Remove or revert builder-side edits to A00-owned programme files after A00 reconciles the useful requests here. Builders should express requests in their own `INTERFACES.md`; A00 owns the central queue. | Open |
| RF-009 | P2 | A01, A02, A03, A04, A05, A06, A07, A08, A09 | Local implementation drafts exist but are not integration-ready. A01 full backend suite passes `628 passed, 1 xfailed` and ruff/mypy pass; A02 durable inbox/job/trace/action plus realtime-webhook acceptance and Activity trace/action endpoint tests pass `14 passed` and focused ruff/mypy pass; A03 Hermes bridge/tool/invoker/run/channel/conformance/native/compat/catalog-MCP/conformance-gate tests pass `84 passed` and focused ruff/mypy pass; A04 connector layer is locally committed with `33 passed`; A05 CS/approval regression suite passes `68 passed` with async SQLite cleanup warnings and focused ruff pass; A06 full Vitest passes `150 passed` and typecheck passes, but whole-project eslint fails two A07-owned `brand/page.tsx` hook errors; A07 operator workspace has backend `6 passed` plus frontend `2 passed`; A08 metric/API/tool/daily-brief tests pass `27 passed`; A09 operations/system/backup/release/extensions/E2E tests pass `54 passed`. These drafts remain unpublished or uncommitted and depend on unaccepted contracts. A01/A02/A04/A05/A08/A09 now each report a branch-local Alembic head from their own draft migration chain, while A07 revises older `e2f9c6b4a1d3`; integration will need explicit migration graph handling. | Owners commit/push coherent slices with living docs, run evidence, current-head or intentional-branch migration metadata, and accepted consumed interfaces. A09 validates migration graph and merge-head handling before integration. | Open |
| RF-010 | P1 | A01, A08, A09 | Duplicate money contract risk. A01 local draft defines canonical `app.core.money.Money` with integer `minor_units` and ISO currency plus tests. A08 local draft separately defines `app.metrics.formulas.Money` with `minor`/`currency`. Because AGENTS I-16 and the engineering specs require typed deterministic money definitions, integration must not preserve two incompatible Money shapes in shared/domain contracts. | A08 either consumes the accepted A01 Money type or the programme records an explicit ADR/interface decision explaining the boundary and conversion. Generated contracts, tests, and metric persistence must use one accepted wire shape before integration. | Open |
| RF-011 | P2 | A03, A09 | Local living docs are stale or internally inconsistent relative to source evidence. A03 is clean at `689cf10` with conformance-gate evidence and `84 passed`, but its local `CURRENT.md` still says `last_verified_commit: 5f971a7` and ends with "no v2 code written yet"; `VERIFICATION.md` also names `5f971a7` as latest despite listing later run/channel evidence. A09 is clean at `6d8b1e3` with operations tests `54 passed`, but `CURRENT.md` still says all builder branches are at baseline even though A01/A03/A04/A09 have local commits and A02/A05/A06/A07/A08 have diffs. | Each owner updates its own living docs to match current source/test state before any ready-for-integration claim. A00 keeps programme status based on inspected source until the owner docs catch up. | Open |
