# Open Review Findings

A00 owns this current queue. Closed findings are removed after the fix is verified and any
lasting rule/test/documentation has been placed in its durable home.

Severity: **P0** blocks all affected integration; **P1** blocks the branch; **P2** must be
scheduled; **P3** is improvement.

| ID | Severity | Affected agent(s) | Finding and evidence | Required exit condition | Status |
|---|---|---|---|---|---|
| RF-001 | P1 | A00–A09, A09 integration | Branch identity is inconsistent inside the active docs. `docs/ecom-os/parallel-build/BRANCH-AND-INTEGRATION.md`, goals, handoffs, and builder living docs name `coordination/program` and `agent/A01-platform-foundation` through `agent/A09-production-integration`. The current launch instruction pins A00 to `agent/a00-orchestrator`, and observed branches are `agent/a01-foundation`, `agent/a02-trace-ledger`, `agent/a03-hermes-runtime`, `agent/a04-cs`, `agent/a05-finance`, `agent/a06-ui-system`, `agent/a07-chat`, `agent/a08-ops-briefs`, and `agent/a09-integration`; current local heads now differ by agent, while `origin` still exposes only A00 programme refs. `origin/main:docs/ecom-os/parallel-build/WORKTREE-SETUP-REPORT.md` and `AGENT-LAUNCH-CARDS.md` confirm these observed branches were intentionally launched, but those files are not present in this branch and the normative branch protocol remains stale. | Either bring the launch-card branch map into the active A00 programme docs and update builder living docs to actual branches, or create/standardize the canonical branch names from the parallel-build docs. A09 ready checks must use one authoritative map. | Open |
| RF-002 | P1 | A02, A04, A05 | Baseline CS external writes bypass the v2 action/attempt/trace contract. `backend/app/services/flow_engine.py:431` calls `self.inbox.send_message(...)` directly and commits only a `TicketMessage`; `backend/app/services/flow_engine.py:367` calls `self.shopify.create_discount(...)` directly; `backend/app/services/connectors/composio_inbox.py:154` executes the Composio send tool directly. There is no durable `actions` row, action digest, connector attempt, provider outcome evidence, or `outcome_unknown` path in this baseline code. | A02 exposes durable action/attempt/trace ports; A04 connector writes require exact connection and attempt recording; A05 reply/discount/refund flows call those ports. Duplicate/race/timeout-after-success/reconciliation tests must prove one supported side effect and visible action state. | Open |
| RF-003 | P1 | A02, A04, A05, A09 | Baseline realtime email webhook bypasses durable event acceptance. `backend/app/api/ecom_webhooks.py:50` accepts `/ecom/webhooks/email`, checks a query/header token at lines 57-60, then starts `_run_loop` as a background task at line 62. It does not verify a provider signature against raw body, persist an inbound event before work, preserve provider event identity, or enqueue Postgres-backed processing before agent work. A02 now has a local unpublished draft that persists realtime email and board webhook triggers into `DurableInboxEvent`/`DurableJob`, includes board-webhook HMAC verification/dispatch, durable/dual worker mode, queue-worker migration/rollback coverage, and passes `40` focused tests, but realtime email still uses shared-secret compatibility auth and none of this is accepted/published branch evidence. | Inbound provider routes verify raw-body signatures, insert a durable inbox event with source/account/provider ID before processing, acknowledge only after durable acceptance, and process through leased jobs with duplicate tests. | Open |
| RF-004 | P1 | A03, A05 | Baseline Hermes integration is the superseded runtime model, not the v2 HermesBridge contract. `backend/app/services/agent_runtime/base.py:1` defines `AgentRuntime`; `backend/app/services/agent_runtime/hermes.py:43` subclasses `LLMCSRuntime`; it posts to `/delegate` at line 83, falls back to direct Anthropic at lines 60-62, and relies on a scoped `cs` profile/no-refund tool assumption at lines 74-80. A03 now has pushed `origin/agent/a03-hermes-runtime` at `4108248` with `HermesBridge`, native blocked stub, OpenClaw compat/dev transport, catalog MCP server, browser chat-gateway allowlist/sanitization, committed HTTP/SSE route, conformance gate runner, compatibility matrix, catalog/capability drift guard, and tool-catalog conformance checks. A00 reran `97 passed` focused A03 tests plus `3 passed` manifest drift guard, ruff, and mypy. The real-Hermes gate still exits `2` BLOCKED because there is no real pinned Hermes endpoint/probe, so no feature is ready under I-19. | A03 publishes current living docs, removes A00-owned programme-file edits from the branch, gets central router/identity/trace dependencies accepted, and tests `HermesBridge` against real pinned Hermes with canonical session create/resume/history, background run recovery, capability probe, adapter/MCP catalog, and honest trace coverage. Legacy `AgentRuntime`/`/delegate` paths are fenced as compatibility input or removed from supported Ecom-OS workflows. Real Hermes capability evidence is required before any feature is marked ready. | Open |
| RF-005 | P1 | A01, A04, A05 | Baseline connector/account selection is not exact enough for supported writes. `backend/app/services/cs_loop.py:67-80` loads stores and uses `stores[0]`, then discovers an active mail account and constructs a Composio inbox connection from the returned ID. `backend/app/services/tickets.py:209-215` repeats first-active mail account discovery. This violates the v2 ban on default/latest/most-recent account selection for supported Ecom-OS writes. | Store, inbox, channel, and connector operations are bound to explicit `brand_id`, `store_id`, `connection_id`, actor, and target before execution. Wrong-account and ambiguous-account fixtures reject in every autonomy mode. | Open |
| RF-006 | P1 | A09, any owner considering legacy feature branches | Legacy feature branches are not safe integration candidates. Relative to baseline `3909904`, `feat/ecom-os-slice-1-design-system` changes 316 files and deletes active v2 agent prompts, programme living docs, builder living docs, design docs, migrations, tests, and many backend modules. `feat/ecom-os-slice-2-connections` changes 286 files and also deletes active v2 prompts/programme/living/design docs plus migrations/tests. | Do not integrate either branch as-is. Any useful code must be manually salvaged by the owning agent into its active branch with current living docs, accepted interfaces, migrations, and tests. | Open |
| RF-007 | P2 | A01, A02, A03, A04, A05, A06, A07, A08, A09 | Builder progress remains mostly local/unpublished. `origin` exposes A00 programme refs and `origin/agent/a03-hermes-runtime`; A03 is no longer blocked by publication for commit `4108248`, although it remains not ready and has additional local uncommitted post-push edits. A01 has local commit `64ca874`; A04 has local commit `46ef05d`; A06 has local commit `a9d870a`; A09 has local commit `272e65c`; A02/A03/A05/A07/A08 have uncommitted worktree diffs. These are useful local evidence but cannot be consumed through `git show origin/<agent-branch>:...` and are not ready-for-integration evidence. | Builders push coherent checkpoints on their assigned branches, including current living docs and verification evidence, or explicitly discard/supersede local drafts. A00 then reconciles programme status against pushed branch evidence. | Open |
| RF-008 | P1 | A03, A04, A06, A00 | Builder worktrees contain edits to A00-owned programme files. A03 commits modify `docs/ecom-os/living/00-program/DECISION-REQUESTS.md` and `INTERFACE-REQUESTS.md`; A06 commit `a9d870a` includes `docs/ecom-os/living/00-program/INTERFACE-REQUESTS.md`; and A04 currently has staged edits to `INTERFACE-REQUESTS.md` and `PROGRAM-STATUS.md` that would overwrite A00's central queue/status with stale placeholder/A04-local content. A08's previous programme-file collision is no longer present in current worktree status, and its useful backend mount request was reconciled as IR-027; A04's useful UI route-state request was reconciled as IR-028. Parallel-build protocol says A00 may edit only `docs/ecom-os/living/00-program/**` and builders keep their own living docs current. | Remove or revert builder-side edits to A00-owned programme files after A00 reconciles the useful requests here. Builders should express requests in their own `INTERFACES.md`; A00 owns the central queue. | Open |
| RF-009 | P2 | A01, A02, A03, A04, A05, A06, A07, A08, A09 | Implementation drafts exist but are not integration-ready. A01 local head `64ca874` passes ruff/mypy, but full pytest could not start in A00's environment because the data volume is at 100% capacity and pytest could not create temp files; previous owner evidence at `f92adbb` was `628 passed, 1 xfailed`. A02 durable inbox/job/trace/action plus realtime-webhook acceptance, board-webhook durable dispatch, Activity trace/action endpoint, and queue-worker migration tests pass `47 passed` and focused ruff/mypy pass, but the draft now touches broad legacy service/connector/CS files. Pushed A03 Hermes bridge/tool/invoker/run/channel/conformance/native/compat/catalog-MCP/chat-gateway/API/conformance-gate/drift-guard evidence passes `97 + 3` tests with focused ruff/mypy pass and the conformance CLI exits `2` BLOCKED without real Hermes; A03 also has local uncommitted post-push edits. A04 connector layer is locally committed with `33 passed`; A05 CS/approval/autonomy/handoff/state-surface/state-API/attention/quality regression suite passes `99 passed` with async SQLite warnings and focused ruff pass, but focused mypy fails; A06 full Vitest passes `150 passed`, typecheck passes, and A06-owned eslint passes, but whole-project eslint fails two A07-owned `brand/page.tsx` hook errors; A07 operator workspace has backend `6 passed` plus frontend `2 passed`; A08 finance/brief/generation/delivery-packet backend suite passes `57 passed`, backend ruff/mypy pass, frontend typecheck/targeted eslint pass, and Finance route Vitest passes `8 passed`; A09 operations/system/backup/release/extensions/E2E tests pass `54 passed`, branch-readiness execution reports every builder NOT READY, and migration graph/head checks pass with env, but branch-readiness static checks fail. These drafts remain unpublished or uncommitted except A03's older pushed checkpoint, and all depend on unaccepted contracts. A01/A02/A04/A05/A08/A09 now each report a branch-local Alembic head from their own draft migration chain, while A07 revises older `e2f9c6b4a1d3`; integration will need explicit migration graph handling. | Owners commit/push coherent slices with living docs, run evidence, current-head or intentional-branch migration metadata, and accepted consumed interfaces. A09 validates migration graph and merge-head handling before integration. | Open |
| RF-010 | P1 | A01, A08, A09 | Duplicate money contract risk. A01 local draft defines canonical `app.core.money.Money` with integer `minor_units` and ISO currency plus tests. A08 local draft separately defines `app.metrics.formulas.Money` with `minor`/`currency`. Because AGENTS I-16 and the engineering specs require typed deterministic money definitions, integration must not preserve two incompatible Money shapes in shared/domain contracts. | A08 either consumes the accepted A01 Money type or the programme records an explicit ADR/interface decision explaining the boundary and conversion. Generated contracts, tests, and metric persistence must use one accepted wire shape before integration. | Open |
| RF-011 | P2 | A01, A02, A03, A05, A06, A09 | Local/pushed living docs are stale or internally inconsistent relative to source evidence. A01 is at `64ca874`, but owner docs still say latest verified commit `f92adbb` and report full-suite evidence for that older commit; A00 observed ruff/mypy pass at `64ca874`, while full pytest could not start because the host data volume is full. A02 owner docs report `46 passed`, while A00 observed `47 passed`; they also need to reflect the current broad service/connector/CS-file diff precisely before any integration claim. A03 is pushed at `4108248` with committed HTTP/SSE route, compatibility matrix, and drift-guard files; A00 observed `97 + 3` passing tests, ruff pass, mypy pass, and conformance CLI exit `2` BLOCKED, but `CURRENT.md` still says `last_verified_commit: 5f971a7`, still ends with "no v2 code written yet", and `VERIFICATION.md` still names `5f971a7` as latest while its aggregate final row reports only `97 passed`. A05 owner docs now report `99 passed`, matching A00's latest regression count, but they still do not record A00's current focused mypy failure across three files. A06 is clean at commit `a9d870a`, but `CURRENT.md` still says `last_verified_commit: 3909904` and `VERIFICATION.md` says the commit is pending/uncommitted evidence. A09 is clean at `272e65c`, but `CURRENT.md` and `VERIFICATION.md` still name older verified commit `a8c0f6d` while the new branch-readiness script fails ruff/mypy. | Each owner updates its own living docs to match current source/test state before any ready-for-integration claim. A00 keeps programme status based on inspected source until the owner docs catch up. | Open |
| RF-012 | P1 | A05 | A05's local CS state/attention/quality work fails focused type checking. `uv run --extra dev python -m mypy app/api/ecom.py app/services/cs_attention_events.py app/services/cs_quality_report.py app/services/cs_state_surface.py app/services/cs_action_handoff.py app/models/cs_autonomy.py app/services/cs_autonomy.py app/services/cs_action_policy.py app/services/cs_action_approvals.py app/services/cs_action_proposals.py` reports 11 errors across `cs_state_surface.py`, `cs_quality_report.py`, and `cs_attention_events.py`: `datetime.desc()` typing, unused ignore, `Sequence` vs `list` mismatches, missing `trace_id` attributes on `QueryModel`, and one `Any` return. Runtime tests pass, but Definition of Done requires type checks. | Fix or intentionally fence the state/attention/quality type errors, then rerun focused mypy with zero errors and update A05 verification docs. | Open |
| RF-014 | P1 | A09 | A09's new branch-readiness tooling is not static-check clean. The read-only gate itself runs and reports all eight builder branches NOT READY, but `uv run --extra dev python -m ruff check ../scripts/ci/branch_readiness.py scripts/check_migration_heads.py` reports unused import `sys`, and `uv run --extra dev python -m mypy ../scripts/ci/branch_readiness.py scripts/check_migration_heads.py` reports `Argument 1 to "join" of "str" has incompatible type "object"; expected "Iterable[str]"` in `branch_readiness.py:179`. | Fix the static errors, rerun ruff and mypy green for the new tooling, and refresh A09 verification docs to the current commit. | Open |
| RF-015 | P1 | A02, A04, A05, A09 | A02's current local durable-core draft crosses source ownership boundaries. The worktree diff now modifies A02-owned durable files but also `backend/app/services/flow_engine.py`, `agent_runtime/*`, connector services such as `connectors/base.py`, `connectors/composio_inbox.py`, `connectors/refunds.py`, `connectors/shopify_direct.py`, and other legacy services owned or consumed by A04/A05/A09. Some changes are formatting-only, but the integration surface is no longer a narrow A02 port/migration slice. Parallel-build rules require builders to stay in assigned ownership and request interfaces/registration instead of editing another agent's owned source. | A02 either narrows the branch to A02-owned ports/migrations/tests or documents and gets explicit owner/interface acceptance for every cross-domain source edit before integration. A09 branch-readiness should reject the branch until ownership and migration effects are explicit. | Open |
