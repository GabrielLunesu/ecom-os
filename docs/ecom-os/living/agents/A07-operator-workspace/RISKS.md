# A07 — Today, Tasks, Knowledge, and Operator Workspace — Current Risks and Edge Cases

| ID | Risk/edge case | Impact | Current mitigation/test | Owner | Status |
|---|---|---|---|---|---|
| A07-R01 | Existing `team_tasks` and legacy board `tasks` may become competing task contracts. | Duplicate APIs, confusing UI, migration conflicts, or lost useful task behavior. | Treat both as migration inputs; choose one canonical v2 A07 task service before source changes; add migration/backfill tests. | A07 | open |
| A07-R02 | Knowledge search may leak restricted document counts, snippets, or titles if filtering happens after search. | Violates access-control invariant and Build Spec Knowledge acceptance. | Local service filters before search/count/snippet; `tests/test_operator_workspace.py` covers viewer vs owner retrieval. Still open until A01 role contract and UI are integrated. | A07/A01 | open |
| A07-R03 | Mutable vault document bodies can overwrite SOP history. | Historical traces cannot identify which SOP version governed an action. | New immutable `KnowledgeDocumentVersion` and supersession test cover local behavior. Still open until trace/evidence integration and old vault compatibility are resolved. | A07/A02 | open |
| A07-R10 | Unsupported documents may be mistaken for searchable knowledge. | Operators or agents could assume a PDF/DOCX was indexed when extraction was unavailable. | Unsupported `document_type` now records `extraction_status=unavailable`/`ingestion_status=unavailable` and is not searched; tests cover unsupported extraction and HTML active-content stripping. Still open until full binary extraction scope is accepted. | A07 | open |
| A07-R04 | Today may treat missing A02/A05/A08 inputs as zero. | False operational calm; restricted counts may leak by absence/presence. | `AttentionInput.source_status` carries unavailable state; backend and frontend tests assert unavailable-not-zero behavior. Still open until real provider adapters replace pending placeholders. | A07 | open |
| A07-R05 | Ranking could drift into LLM/prose heuristics. | Non-reproducible attention order and untestable explanations. | `rank_attention()` uses deterministic scoring/reason codes with unit coverage; `/overview` renders the returned reasons. Still open until full source composition is implemented. | A07 | open |
| A07-R06 | Contextual Ask Hermes could duplicate A03 chat transport or pass raw document/task content. | Breaks Hermes-native chat invariant and may leak restricted content. | A07 emits safe launch intents only; backend and frontend tests assert no body/transcript/credential keys, and the UI now shows prepared intent metadata without opening chat. Still open until A03 accepts/executes launch contract. | A07/A03 | open |
| A07-R07 | Frontend route work may fork A06 primitives or global navigation. | Merge conflicts and inconsistent operational states. | Wait for A06 contract for shell/primitives; keep A07 UI changes route-local. | A07/A06 | open |
| A07-R08 | Agent-created tasks without actor/run/trace provenance become ordinary data. | Loses auditability and violates A07 acceptance. | `OperatorTaskCreate` rejects agent tasks without actor/run/trace; test covers rejection and persisted provenance. Still open until A02/A03 IDs are accepted. | A07/A02/A03 | open |
| A07-R09 | Task access labels may diverge from the accepted A01 role model. | Task counts/titles could leak across roles or require migration when A01 lands. | Local role-to-access-label filtering now applies server-side to task list/get/update/comment paths; the Tasks UI sends an explicit role for list/update/comment calls; tests cover hidden restricted tasks/comments and frontend role-scoped task requests. Still open until A01 publishes canonical access labels. | A07/A01 | open |
| A07-R11 | Integration may reject the branch because final repo-wide gates were skipped or failed outside A07 scope before push. | A09/A00 may require another checkpoint before merge despite local A07 tests passing. | `VERIFICATION.md` records exact targeted passes, skipped full mypy/eslint/migration readiness by user instruction, missing `scripts/ci/branch_readiness.py`, and non-A07 formatting failures. | A07/A09 | open |

Delete resolved rows after the durable behavior/test/documentation is in place. This is
not an incident history.
