# A03 — Hermes Native Integration and Main Chat — Current Risks and Edge Cases

| ID | Risk/edge case | Impact | Current mitigation/test | Owner | Status |
|---|---|---|---|---|---|
| A03-R01 | **OpenClaw vs Hermes identity unresolved.** Existing 7.1k-LOC gateway speaks OpenClaw WS JSON-RPC v3 + device pairing; specs pin Hermes `v0.16.0`/`v2026.6.5` NousResearch TUI Gateway. Unknown if OpenClaw is renamed Hermes or a separate product. | Determines how much transport code is reused vs newly written; wrong assumption rebuilds or wrongly reuses the bridge core. | None yet. Decision request to be filed; treat as separate until proven. Build `HermesBridge` against documented Hermes contract; keep OpenClaw seams (`GatewayControlPlane`, retry, session-keys) as reference only. | A03 (+ human/ADR) | open |
| A03-R02 | **No pinned Hermes runtime available to probe.** Spec requires capability probe + conformance against a real pinned release; none installed in this environment (`HERMES_GATEWAY_URL` unset). | Cannot mark any Hermes-dependent feature `ready`; risk of coding to assumed protocol shapes. | Build bridge behind typed ports + fakes + conformance fixtures; gate `ready` on real probe (I-19). Record assumptions explicitly. | A03 | open |
| A03-R03 | **`/delegate` spike mistaken for architecture.** `agent_runtime/hermes.py` + `cs_llm.py` POST to a hypothetical `/delegate` HTTP endpoint. | Adopting it as the chat/run transport violates Common Trap #3 (no sessions/stream/history/interrupt). | Classified `obsolete-as-architecture` in `CURRENT.md`; keep only as degraded fallback concept; replace with `HermesBridge`. | A03 | open |
| A03-R04 | **Hand-maintained MCP/adapter tool schemas drift.** Current MCP tools are inline in `server.py`; CS tools inline in `llm.py:78`. | Two/three hand-edited copies diverge → schema-hash mismatch, money-touching arg misread. | Single canonical catalog generates adapter+MCP+validation+fixtures+hash (Runtime Spec §6.1); test asserts no refund/cancel tool name. | A03 | open |
| A03-R05 | **Browser over-privilege.** Upstream gateway protocol exposes `config.set`, `exec`, `cli.exec`, secret/sudo responses; current generated client surfaces broad methods. | Proxying these to the browser = critical security break (AGENTS §3, Runtime §4.1). | Web client exposes only product-approved methods; no service credential to browser; sudo/secret never via ordinary chat UI in v1. Invariant test. | A03 | open |
| A03-R06 | **Coverage mislabeling.** A Hermes hook seeing a native terminal/browser/3rd-party tool call could be labeled `verified`. | Dishonest trace metrics (I-12), false audit completeness. | Only Ecom-OS-endpoint-handled invocations are `verified`; native activity is `observed`/`unknown`. Conformance test §15.3. | A03 (with A02) | open |
| A03-R07 | **Reconnect infers completion.** Lost WS/SSE socket treated as run finished. | Wrong terminal state, missed actions, duplicate runs. | Reconnect queries real status/history; background worker polls run before retry; lease loss re-checks state (I-07/I-08, Runtime §13.3). | A03 | open |
| A03-R08 | **Branch-name mismatch.** Goal/handoff/frontmatter said `agent/A03-hermes-integration`; actual branch is `agent/a03-hermes-runtime`. | Integration tooling keyed on the wrong branch could miss this work. | Frontmatter + `CURRENT.md` corrected to real branch; flagged to programme. Do not switch branches. | A03 | open |

Delete resolved rows after the durable behavior/test/documentation is in place. This is not
an incident history.
